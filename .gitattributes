# Normalize line endings so the repo is consistent across Windows/macOS/Linux.
* text=auto eol=lf

# Datasets and figures are assets — don't let editors reformat them.
*.jsonl text eol=lf
*.svg   text eol=lf
*.png   binary
*.gif   binary
